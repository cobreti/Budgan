export enum WorkspaceFolderTypeNames {
    rawFiles = 'rawFiles',
    files = 'files',
    accounts = 'accounts',
    transactions = 'transactions',
    transactionsMetadata = 'transactionsMetadata',
    accountsMetadata = 'accountsMetadata',
}

export enum WorkspaceFileNames {
    metadata = 'metadata.json',
}

